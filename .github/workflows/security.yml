name: security

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  basic-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/basic_secret_scan.sh
