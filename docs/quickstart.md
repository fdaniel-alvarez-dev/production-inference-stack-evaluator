# Quickstart

```bash
python -m venv .venv
source .venv/bin/activate
make setup
make example
```

Open `reports/minimal_report.md` after the example finishes.

The bundled metrics are illustrative sample values only. They are not benchmark claims.
